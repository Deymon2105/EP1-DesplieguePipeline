spring:
  application:
    name: ms-productos-tcg
  datasource:
    url: ${DATABAS_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

server:
  port: ${SERVER_PORT:9005}